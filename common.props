<Project>
	<Import Project="version.props" />
	<PropertyGroup>
		<TargetFrameworks>$(MicrosoftTargetFramework)</TargetFrameworks>
		<ImplicitUsings>enable</ImplicitUsings>
		<Nullable>enable</Nullable>
		<Version>$(Version)</Version>
	</PropertyGroup>
</Project>